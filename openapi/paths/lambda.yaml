get:
  tags:
    - Lambda
  summary: Get currency code
  description: Get currency code for the authenticated user (Lambda system only)
  operationId: getCurrencyCode
  parameters:
    - $ref: '../components/parameters/index.yaml#/ApiKeyHeader'
  security:
    - ApiKeyAuth: []
  responses:
    '200':
      description: Successfully retrieved currency code
      content:
        application/json:
          schema:
            $ref: '../components/schemas/LambdaResponse.yaml'
    '401':
      description: Unauthorized - Invalid API key
    '500':
      $ref: '../components/responses/index.yaml#/InternalServerError'