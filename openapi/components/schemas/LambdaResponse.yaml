type: object
required:
  - currencyCode
  - region
properties:
  currencyCode:
    type: string
    description: User's currency code
    example: "USD"
  region:
    type: string
    description: User's region
    example: "US"