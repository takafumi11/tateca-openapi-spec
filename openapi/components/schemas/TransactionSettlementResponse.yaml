type: object
required:
  - settlements
  - totalAmount
properties:
  settlements:
    type: array
    items:
      $ref: './Settlement.yaml'
  totalAmount:
    type: number
    format: decimal
    description: Total amount involved in settlements
    example: 245.75