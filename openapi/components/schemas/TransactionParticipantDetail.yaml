type: object
required:
  - user
  - amount
  - settled
properties:
  user:
    $ref: './UserResponse.yaml'
  amount:
    type: number
    format: decimal
    description: Amount owed by this participant
    example: 30.25
  settled:
    type: boolean
    description: Whether this participant has settled their debt
    example: false